{
  "name": "@dogule/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite || echo ERR_WEB_START_001",
    "build": "vite build || echo ERR_WEB_BUILD_001",
    "preview": "vite preview || echo ERR_WEB_PREVIEW_001",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --reporter=dot",
    "e2e": "npx playwright test"
  },
  "dependencies": {
    "@dogule/domain": "workspace:*",
    "@dogule/testing": "workspace:*",
    "@dogule/utils": "workspace:*",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.4.0",
    "@testing-library/react": "^14.0.0",
    "@vitejs/plugin-react": "^5.0.0",
    "vitest": "^1.6.1",
    "vite": "^5.2.0"
  }
}
